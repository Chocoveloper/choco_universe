// Archivo: lib/config/choco_config.dart

class ChocoConfig {
  static const String version = "1.0.0";
  static const String buildName = "Milky Way Classic";
  
  // 🚩 LA BANDERA DEL CHOCO-WOW
  // false = Versión clásica (La que le mostrarás primero)
  // true = Versión Épica (Se activan las órbitas, estrellas y radar NEO avanzado)
  static const bool showChocoWowFeatures = false; 
}